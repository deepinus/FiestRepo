{
  "name": "airport-companion-app",
  "version": "1.0.0",
  "description": "A marketplace for connecting vulnerable travelers with fellow passenger companions",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
